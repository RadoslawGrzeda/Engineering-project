# Legacy keys (kept for compatibility — note the space before '=' is intentional)
minIO_login ='CHANGE_ME_minio_user'
minIO_password='CHANGE_ME_minio_password'

API_KEY = "CHANGE_ME_api_key"
Secret_Key = "CHANGE_ME_secret_key"

# Streamlit basic-auth users. Generate bcrypt hashes, e.g.:
#   python -c "import bcrypt;print(bcrypt.hashpw(b'yourpass',bcrypt.gensalt()).decode())"
name=user
password=CHANGE_ME_bcrypt_hash
admin_name=admin
admin_pass=CHANGE_ME_bcrypt_hash

# MinIO root credentials (used by the minio container)
MINIO_USER=CHANGE_ME_minio_user
MINIO_PASSWORD=CHANGE_ME_minio_password

MINIO_ADDRESS=minio-api.example.com
