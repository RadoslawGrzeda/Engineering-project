[pytest]
# pytest 9.0.2 + Python 3.13: assertion rewriter fails on complex pandas
# boolean expressions (~a | ~b inside subscripts) in streamlit_app.py.
# Plain assertions still produce correct PASS/FAIL results.
addopts = --assert=plain
