# Backup MinIO credentials
MINIO_BACKUP_USER=backup-admin
MINIO_BACKUP_PASSWORD=CHANGE_ME_backup_minio_password

# Primary MinIO credentials (for mc mirror) — must match minIO/.env
MINIO_PRIMARY_USER=CHANGE_ME_minio_user
MINIO_PRIMARY_PASSWORD=CHANGE_ME_minio_password
